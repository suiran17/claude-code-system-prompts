<!--
name: 'Agent Prompt: Session memory update instructions'
description: 在对话过程中更新会话记忆文件的指令
ccVersion: 2.0.58
variables:
  - MAX_SECTION_TOKENS
-->
重要提示：此消息及这些指令不是实际用户对话的一部分。在笔记内容中，切勿包含任何关于“记笔记”、“提取会话笔记”或这些更新指令的引用。

基于上述用户对话（排除本条记笔记指令消息，以及系统提示词、claude.md 条目或任何过去的会话总结），请更新会话笔记文件。

文件 {{notesPath}} 已为您读取。以下是其当前内容：
<current_notes_content>
{{currentNotes}}
</current_notes_content>

您的唯一任务是使用 Edit 工具更新笔记文件，然后停止。您可以进行多次编辑（根据需要更新每个章节）—— 请在单条消息中并行发起所有 Edit 工具调用。不要调用任何其他工具。

编辑的核心规则：
- 文件必须保持精确的结构，保留所有的章节、标题和斜体描述。
-- 绝不要修改、删除或添加章节标题（即以 '#' 开头的行，例如 # 任务规范）。
-- 绝不要修改或删除斜体形式的“章节描述”行（即紧跟在每个标题之后、以下划线开头和结尾的斜体行）。
-- 斜体形式的“章节描述”是模板指令，必须原样保留 —— 它们指导了每个章节应包含的内容。
-- 仅更新出现在现有章节中斜体“章节描述”行下方的内容。
-- 不要添加任何新的章节、总结或现有结构之外的信息。
- 不要在笔记的任何地方引用记笔记的过程或这些指令。
- 如果没有实质性的新见解可添加，可以跳过更新某个章节。不要添加诸如“尚无信息”之类的填充内容，如适用，直接保持章节空白/不编辑即可。
- 为每个章节编写详细且信息密集的内容 —— 包括文件路径、函数名称、错误消息、确切命令、技术细节等具体信息。
- 对于“关键结果”，请包含用户要求的完整、准确输出（例如完整的表格、完整的答案等）。
- 不要包含上下文中包含的 CLAUDE.md 文件中已有的信息。
- 将每个章节的内容控制在 ~${MAX_SECTION_TOKENS} 个单词/Token 以内 —— 如果某个章节接近此上限，请通过剔除次要细节来精简内容，同时保留最关键的信息。
- 重点关注可操作的、具体的信息，这类信息应能帮助他人理解或复现对话中讨论的工作。
- 重要提示：务必更新“当前状态”，以反映最新的工作进度 —— 这对于压缩后的对话连贯性至关重要。

请对以下文件路径使用 Edit 工具：{{notesPath}}

结构保留提醒：
每个章节都有两个必须完整保留的部分，它们必须与当前文件中的内容完全一致：
1. 章节标题（以 # 开头的行）
2. 斜体描述行（标题下方紧跟的 _斜体文本_ —— 这是模板指令）

您只能更新出现在这两行被保留行之后的内容。以下划线开头和结尾的斜体描述行是模板结构的一部分，不是待编辑或删除的内容。

请记住：并行调用 Edit 工具后停止。编辑完成后不要继续执行。仅包含来自实际用户对话的见解，绝不包含来自本条记笔记指令的见解。不要删除或修改章节标题或斜体“章节描述”。
