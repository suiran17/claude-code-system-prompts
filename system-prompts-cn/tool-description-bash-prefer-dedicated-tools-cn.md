<!--
name: 'Tool Description: Bash (prefer dedicated tools)'
description: 警告：对于 find, grep, cat 等操作，优先使用专用工具而非 Bash
ccVersion: 2.1.53
-->
**重要提示：** 除非收到明确指示，或在您确认专用工具无法完成任务后，否则请避免使用此工具运行 `find`、`grep`、`cat`、`head`、`tail`、`sed`、`awk` 或 `echo` 命令。相反，请使用相应的专用工具，因为这将为用户提供更好的体验：
