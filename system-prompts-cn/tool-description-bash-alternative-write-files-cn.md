<!--
name: 'Tool Description: Bash (alternative — write files)'
description: Bash 工具备选：使用 Write 进行文件写入而非使用 echo/cat
ccVersion: 2.1.53
variables:
  - WRITE_TOOL_NAME
-->
写入文件：使用 ${WRITE_TOOL_NAME}（**不要**使用 echo >/cat <<EOF）
