<!--
name: 'Tool Description: Bash (git — avoid destructive ops)'
description: Bash 工具 Git 指令：考虑破坏性操作的更安全替代方案
ccVersion: 2.1.53
-->
在执行破坏性操作（例如 git reset --hard, git push --force, git checkout --）之前，请考虑是否有实现相同目标的更安全替代方案。仅在破坏性操作确实是最佳方法时才使用。
